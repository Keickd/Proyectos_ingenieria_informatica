package es.usj.androidapps.alu100485.movieslibrary.fragments

//This is for checking later which tab i selected, so what fragment
class MyFragment {
    var fragmentMovies: FragmentMovies = FragmentMovies()
    var fragmentActors: FragmentActors = FragmentActors()
    var fragmentGenres: FragmentGenres = FragmentGenres()
}